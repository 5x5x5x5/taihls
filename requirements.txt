# For cloud runners (Binder) that can't read pyproject.toml.
# The pinned source of truth is pyproject.toml + uv.lock; keep these in sync.
# Light tier only — what the in-book code cells need. Heavyweight libs (torch,
# transformers, ...) are installed inside the companion Colab notebooks themselves.
jupyter-book>=2
matplotlib
numpy
pandas
scikit-learn
ipykernel
