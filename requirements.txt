# For cloud runners (Binder / Colab) that can't read pyproject.toml.
# The pinned source of truth is pyproject.toml + uv.lock; keep these in sync.
# Only the packages the code-cells need to execute — all Pyodide-compatible.
jupyter-book>=2
matplotlib
ipykernel
